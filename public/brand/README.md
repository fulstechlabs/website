# Brand asset provenance

The mark and favicon in this directory were copied from
`fulstechlabs/brand` brand version `0.3.0`.

They are local build inputs so the public website does not depend on the
private brand repository at runtime.

